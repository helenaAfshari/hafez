import 'package:flutter/material.dart';

class ProgressBarWidgetMusic extends StatelessWidget {
  const ProgressBarWidgetMusic({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}